{
  "name": "athena",
  "version": "0.1.0",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.82.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}