# Публикация каталогов в GitHub репозиторий

## Обзор
Плагин Athena теперь поддерживает прямую публикацию нормализованных JSON-каталогов в GitHub репозиторий `ackedze/design-system_ab`.

## Как использовать

### 1. Подготовка GitHub токена
Для публикации каталогов вам потребуется GitHub Personal Access Token:

1. Перейдите на https://github.com/settings/tokens
2. Нажмите "Generate new token"
3. Выберите scope `repo` (полный доступ к репозиториям)
4. Скопируйте полученный токен

### 2. Сбор и публикация каталога
1. В плагине Athena нажмите "Собрать компоненты"
2. После сбора будут видны все компоненты
3. Нажмите кнопку "Опубликовать" 
4. Если это первая публикация, появится окно для ввода GitHub токена
5. Введите ваш токен и нажмите "Сохранить"
6. Каталог будет опубликован в репозиторий под именем в формате: `catalogs/{catalog-name}.json`

### 3. Где найти опубликованный каталог
После успешной публикации, файл появится в:
```
https://github.com/ackedze/design-system_ab/blob/main/catalogs/{catalog-name}.json
```

## Структура файлов в репозитории
Опубликованные каталоги хранятся в директории `catalogs/` репозитория:
```
design-system_ab/
  catalogs/
    my-design-catalog.json
    another-catalog.json
    ...
```

## Имена каталогов
Имя каталога автоматически формируется из:
- Имя библиотеки (если указана в метаданных)
- Имя страницы из Figma

Пример: `library -- page-name`

## Безопасность
- GitHub токен сохраняется локально в браузере (localStorage)
- Токен отправляется только при публикации каталога
- Рекомендуется использовать токены с ограниченными правами доступа

## Обновление существующего каталога
Если каталог с таким же именем уже опубликован, он будет обновлен с новыми данными. История изменений сохраняется в Git.

## Возможные проблемы

### "GitHub token не найден"
- Убедитесь, что ввели токен в диалоге
- Проверьте, что браузер позволяет использовать localStorage
- Попробуйте еще раз: нажмите "Опубликовать" → введите токен

### Ошибка "401 Unauthorized"
- Проверьте, что токен введен правильно
- Убедитесь, что у токена есть scope `repo`
- Проверьте срок действия токена

### Ошибка "403 Forbidden"
- У используемого токена нет доступа к репозиторию
- Убедитесь, что токен имеет scope `repo`

### Ошибка "CORS error"
- Это не должно происходить для Figma плагинов
- Если произойдет, обновите плагин до последней версии
